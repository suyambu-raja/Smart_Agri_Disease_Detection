# ──────────────────────────────────────────────
# Smart Agriculture – Environment Variables
# Copy this file as .env and fill in real values
# ──────────────────────────────────────────────

# Django
DJANGO_SECRET_KEY=your-super-secret-key-change-in-production
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# Firebase – path to your service-account JSON
FIREBASE_CREDENTIALS_PATH=firebase_credentials.json

# CORS – comma-separated origins
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Model paths (relative to BASE_DIR / models/)
DISEASE_MODEL_PATH=models/disease_model.h5
YIELD_MODEL_PATH=models/yield_model.pkl
