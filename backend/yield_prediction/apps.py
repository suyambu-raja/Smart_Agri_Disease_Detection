from django.apps import AppConfig


class YieldPredictionConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'yield_prediction'
    verbose_name = 'Crop Yield Prediction'
